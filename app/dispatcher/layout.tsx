import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Панель диспетчера — Ремонтная служба',
}

export default function DispatcherLayout({ children }: { children: React.ReactNode }) {
  return children
}
