import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Вход — Ремонтная служба',
}

export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return children
}
